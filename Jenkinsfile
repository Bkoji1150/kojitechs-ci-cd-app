pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                echo 'git clone is happening yes'
            }
        }
         stage('mvn Compile and Build') {
            steps {
                echo 'mvn Compile and Build'
            }
        }
    }
}
